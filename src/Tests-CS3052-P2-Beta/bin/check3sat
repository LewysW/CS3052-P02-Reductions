#!/bin/bash
#

DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" >/dev/null 2>&1 && pwd )"

export k=`$DIR/checksat < /dev/stdin`
export RESULT=$?

if [[ $RESULT != 0 ]] ; then
	exit $RESULT
	fi

if [[ "$k" -gt "3" ]] ; then
	echo "Error: clause longer than 3"
	exit 1
	fi

echo $k

exit 0

